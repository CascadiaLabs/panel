{
  "name": "panel-web",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "rm -rf ../api/static && mkdir -p ../api && cp -R dist ../api/static",
    "preview": "vite preview"
  },
  "dependencies": {
    "@xyflow/svelte": "^1.6.6",
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
